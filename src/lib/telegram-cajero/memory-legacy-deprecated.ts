/**
 * Deprecated: memoria legacy en Map reemplazada por `src/lib/telegram-cajero/memory/index.ts`.
 *
 * No importar este archivo desde el bot vivo. Se conserva solo como nota de
 * migracion para evitar reintroducir el conflicto `memory.ts` vs `memory/`.
 */

export const TELEGRAM_CAJERO_LEGACY_MEMORY_DEPRECATED = true
